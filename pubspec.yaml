name: awi_stream_app
description: Crunchyroll clone for AWI.TO
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0             # Internet se Firebase data laane ke liye
  cached_network_image: ^3.3.0 # HD Posters ko fast load karne ke liye

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
